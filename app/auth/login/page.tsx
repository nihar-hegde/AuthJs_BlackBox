// NOTE: here we will render the LOGINFORM component

import { LoginForm } from "@/components/auth/login-form";

const LoginPage = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
